# attic — preserved history, not live code

Early spikes and superseded demos, kept for the record. Nothing in the live suites or
deployed services references this directory.

| what | superseded by |
|---|---|
| `spikes/` | the spike conclusions live in ARCHITECTURE.md; 01-jspi-economics → `runtime/scheduler.mjs`, 03-durable-statem → `durable-genserver/` |
| `cloudflare-workers/` | early Durable Object workers (todo/portfolio/order-risk) → `durable-genserver/`, `demo/durable-sql/` |
| `durable-object/` | the original account-DO demo → `durable-genserver/` (deployed) |
| `jason-demo/` | the first real-dep build → `demo/markdown/` (deployed, byte-exact gate) |
| `measurements/` | early one-off measurement notes → `perf/` (reproducible harnesses) |
| `TODO.txt` | the original handoff plan — fully executed; see WRITEUP.md for where it ended up |
